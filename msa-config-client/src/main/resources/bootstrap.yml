spring:
  application: 
    name: config-client
    
server:
   port: 8080

management:
   server:
      security:
         enabled: false
   endpoints:
      web:
         exposure:
            include: '*'
         cors:
            allowed-origins: http://example.com
            allowed-methods: GET,POST

book:
   author: dinstone
   name: springcloud
   category: category

